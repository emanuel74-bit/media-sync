{
    "name": "media-sync-architecture",
    "private": true,
    "description": "LikeC4 architecture model for the media-sync platform (ADR-0006 trial). Requires Node >= 22.",
    "engines": {
        "node": ">=22"
    },
    "scripts": {
        "dev": "likec4 start",
        "build": "likec4 build -o dist",
        "export": "likec4 export png -o export"
    },
    "devDependencies": {
        "@rolldown/binding-win32-x64-msvc": "^1.1.1",
        "likec4": "^1.58.0"
    }
}
